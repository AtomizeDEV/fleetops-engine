import Controller, { inject as controller } from '@ember/controller';
import { inject as service } from '@ember/service';
import { tracked } from '@glimmer/tracking';
import { action } from '@ember/object';
import { isBlank } from '@ember/utils';
import generateSlug from '@atomizedev/ember-core/utils/generate-slug';
import Point from '@atomizedev/fleetops-data/utils/geojson/point';

export default class ManagementDriversIndexOrdersEditController extends Controller {

}
